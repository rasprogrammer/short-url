
/*
    id - auto increment , primary key 
    user_id 
    original_url 
    short_code
    custom_alias
    domain_id
    is_active
    expires_at
    password_protected
    password_hash

    click_count

    created_at
    updated_at

    // index(short_code, custom_alias, user_id)

*/